# سيناريو العرض — منصة تفوّق (ديمو ~10 دقائق)

شغّل `npm run dev` وافتح http://localhost:3000 — أو على شاشة ثانية بالشبكة: `http://<IP>:3000`

## قبل العرض (30 ثانية)
1. افتح `/login` → اضغط «إعادة تعيين بيانات الديمو» (تحت حسابات الديمو) — بداية نظيفة
2. جهّز كود الخصم: `KUWAIT20` (خصم 20%) أو `AHLAN10`

## الفصل ١: الطالب المشترك — أحمد (٣ دقائق)
**القصة:** المنصة مش بس محتوى — بتخلي الطالب يدمن المذاكرة.

1. `/login` → «دخول كطالب» (أحمد — مشترك)
2. الداشبورد: حلقة المستوى + XP + **الستريك اليومي** + هدف اليوم — «دي ميكانيكية الألعاب»
3. بطاقة «كمّل من وقفت» → افتح درس → فيديو + مذكرة + زر «تحميل PDF» (يطبعها PDF)
4. «ابدأ الاختبار» → اختبار موقوت → سلّم → **تصحيح فوري + مراجعة الإجابات + XP**
5. «تقاريري» → نقاط القوة والضعف لكل مادة

## الفصل ٢: دورة الفلوس — سارة (٣ دقائق) ⭐ أهم لحظة
**القصة:** المحتوى محمي — والدفع بيفتح كل حاجة لحظيًا.

1. اخرج → «دخول كطالبة» (سارة — اشتراكها منتهي)
2. «المواد» → افتح الرياضيات → لاحظ: أول درس **مجاني**، الباقي **مقفول 🔒**
3. ادخل درس مقفول → شاشة «محتوى حصري للمشتركين»
4. «اشترك الآن» → اختر «باقة المرحلة» → طبّق `KUWAIT20` → السعر ينزل 19.9 ← 15.92 د.ك
   - *لو سألك عن «باقة المادة»:* بتفتح مادة واحدة تختارها وقت الدفع — باقي المواد تفضل مقفولة
5. ادفع بـ **KNET** → «تم الدفع بنجاح»
6. ارجع لنفس الدرس → **اتفتح** — الفيديو والمذكرة والاختبار شغالين

## الفصل ٣: ولي الأمر — خالد (دقيقتان)
1. اخرج → «دخول كولي أمر»
2. كارت كل ابن: المعدل + الأداء حسب المادة + آخر الاختبارات
3. **الإشعارات**: نتائج أحمد وسارة + «سارة بدون اشتراك» (تتحدث حيًا)
4. ⭐ كارت سارة → زر **«اشترك الآن»** الذهبي → اختر باقة + كود خصم + KNET → **اشتراكها يتفعّل فورًا** — ولي الأمر يدفع بنفسه
5. الجرس في الهيدر — عداد غير المقروء

## الفصل ٤: الإدارة — دقيقتان
1. اخرج → «دخول كمدير»
2. نظرة عامة: إيرادات **حية** (دفعة سارة الـ 15.92 ظاهرة) + طلاب + محتوى (750 درس، 4500 سؤال)
3. «المنهج والمحتوى» → أضف درس/مادة → **تظهر للطالب فورًا** — «العميل يدير المحتوى بنفسه»
4. «بنك الأسئلة» → أضف سؤال لدرس → يدخل الاختبار مباشرة
5. «المبيعات» → سجل المدفوعات كامل (KNET/Visa) — «دي نفس بيانات بوابة الدفع الحقيقية»

## الختام (٣٠ ثانية)
> «كل اللي شفتوه شغال دلوقتي — قاعدة بيانات حقيقية في النسخة النهائية، بوابة دفع كويتية معتمدة، ونفس التجربة دي على الموبايل.»

## طوارئ أثناء العرض
- حاجة اتلخبطت؟ `/login` → «إعادة تعيين بيانات الديمو» → ترجع كل حاجة زي الأول
- السيرفر واقف؟ `npm run dev` في `platform-poc` — بيفتح على 3000
- الشاشة التانية على نفس الواي فاي: `http://192.168.137.1:3000`
